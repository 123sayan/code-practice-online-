public class Solution {
    public static int shakeHands(ArrayList<Integer> arr , int n, int k) {
        // Write your code here.
        for(int i=0;i<n;i++)
        {
            if(arr.get(i)==k)
            {
                k+=arr.get(i);
            }
        }
        return k;
    }
}
