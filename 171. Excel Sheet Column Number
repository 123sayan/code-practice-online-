class Solution {
    public int titleToNumber(String str) {
        int ans=0;
        for(int i=0;i<str.length();i++)
        {
            ans=ans*26+str.charAt(i)-'A'+1;
        }
        return ans;
    }
}
